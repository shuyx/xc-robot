# XC-ROBOT 主要依赖配置
# 这是运行XC-ROBOT系统的核心依赖列表

# 导入核心依赖
-r requirements/base.txt

# 可选功能依赖（按需安装）:
# pip install -r requirements/dev.txt          # 开发工具
# pip install -r requirements/web.txt          # Web界面服务
# pip install -r requirements/monitoring.txt   # 监控图表
# pip install -r requirements/simulation_vtk.txt  # VTK 3D仿真（不推荐，建议使用Web端方案）
# pip install -r requirements/camera.txt          # 相机工具

# 机器人控制（如果可用）
# fairino  # 通过本地路径导入
# hermes-api  # 通过HTTP API调用

# 注意：
# 1. VTK依赖已移至可选安装，节省380M空间
# 2. 推荐使用requirements/目录下的分层依赖管理
# 3. 如需3D仿真，建议使用Web端Three.js方案